# Xcode
*.xcuserdata/
*.xcworkspace/
DerivedData/
build/

# Swift Package Manager
.build/
Packages/

# OS
.DS_Store

# Claude Code — never commit to public repos
.claude/
.agents/
CLAUDE.md
skill-lock/
skills-lock.json
APP_STORE_SUBSCRIPTIONS.md

# Firebase — contains API key, do not commit to public repos
# Copy GoogleService-Info.plist.template → GoogleService-Info.plist and fill in your values
GoogleService-Info.plist

# Scratch / temp files
scratch/

# Secrets / local config (never commit real keys)
Secrets.swift
*.secret
*.env
